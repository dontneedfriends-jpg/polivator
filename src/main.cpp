#include <Arduino.h>

void setup() {
  // Initialize serial communication
  Serial.begin(115200);
  // Initialize sensors, display, web server, etc.
}

void loop() {
  // Main loop: read sensors, update display, handle web requests, etc.
}
